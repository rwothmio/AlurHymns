import { StyleSheet } from "react-native";
import sv from "./styleVariables";

const styles = StyleSheet.create({
  title: {
    fontSize: 16,
    color: sv.txt2,
  },
  sectionPadding: {
    paddingHorizontal: sv.s2,
    paddingVertical: sv.s3,
  },
});

export default styles;
